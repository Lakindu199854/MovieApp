//Boiler plate code is genereated using rafce

import React from "react";
import { Outlet } from "react-router-dom";

const Layout = () => {
  return (
    <div>
      <div>Layout</div>
      <Outlet></Outlet>
    </div>
  );
};

export default Layout;
